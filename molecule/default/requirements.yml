---
# The role itself pulls in nothing beyond what the `ansible` distribution
# already bundles (it uses `ansible.posix.authorized_key`). This is what
# Molecule's own Docker driver needs in order to create and destroy the test
# container.
collections:
  - name: community.docker
    version: 5.2.2
